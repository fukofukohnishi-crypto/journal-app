# SPEC-A｜記録

**このファイルはアプリA専用。** 共通事項は `SPEC.md` を参照すること。
やらないことリスト（SPEC.md セクション2）とデザイン仕様（同5）は厳守。

**アプリAはAPIを使わない。** 外部通信ゼロ。データは全て端末内。

---

## 実装順

1. 記録画面 ＋ 蓄積ビジュアル
2. PWA化（manifest.json ＋ アイコン）→ **ここで一度手を止めて、数日実際に使う**
3. 予行演習の答え合わせ
4. 心配ごとの一覧
5. 土曜モード

---

## 1. 記録画面

上から順に答える一画面構成。**答えやすい順に降りていく。**
上二つが数値、次二つが選択、最後が言葉。最初の2つを動かして閉じるだけでも成立する。

### ① 体の調子（0〜100%）

スライダー＋%表示。**数値を表示してよい。**
体調は評価ではなく事実なので、数値化しても自己否定に転びにくい。

- 補助テキスト（「重い／軽い」等）は**置かない**。項目名で伝わる
- **%の数字はラベルの下に左寄せ**。右上に大きく置くと目が散る

### ② 心の余白（0〜100%）

同じくスライダー＋%表示。補助テキストは置かない。

※この2軸は「身体の残量」と「時間の残量」。どちらも詰まっていないかを測っている。重ねると、余白のない日が続いたあとに調子が落ちる、といった関係が見える。

### ③ 頭にあること（複数選択）

`仕事` `子供` `家のこと` `自分の体・健康` `人との関係` `先のこと` `楽しみなこと` `わからない`

**「原因」を問わない。**今そこにあるものを置くだけ。原因の特定は苦手領域なので、特定を求めない言葉にしている。

- **「先のこと」は予行演習の受け皿。**これが多い週は先回りが走っている週。ここが答え合わせ（セクション3）の入口になる
- **「自分の体・健康」は健康不安の置き場所**（19）
- **「楽しみなこと」は唯一の正の項目。**全部が負だと開くのが重くなる
- **「わからない」は必須。**感情がわからないことが出発点なので、それが選べないアプリは意味がない

#### 選んだ項目ごとに、一行だけ書ける

項目を選ぶと、その下に**一行の入力欄**が出る。複数選べば複数出る。

> 何が気になってる？

- **書かなくてもよい。**空のまま置ける
- **一行。文章を書かせない。**長く書かせると咀嚼が始まり、感情が書き換わる（2）
- ここに書かれた文字が、心配ごとの一覧（セクション4）に溜まる。**この入力口がないと一覧が機能しない**

### ④ 今日思えたこと（複数選択・任意）

**行動ではなく、思考の変化を積む。**
行動を積むとチェックリストになり、タスク管理と同じ構造になる（9）。また行動は毎日ほぼ同じなので飽きる。
思考は毎日違うので飽きにくく、何より**変えたいのはそこ**。

11項目すべて、これまでの課題の裏返しになっている。

**引き算（手放せた）**

`まあいいか` `これでじゅうぶん` `やらなくて平気だった` `心配しすぎだった` `目の前のことだけ見てた` `人の目が、あまり気にならなかった`

**足し算（出せた・感じられた）**

`わからないって言えた` `それ、私は違うな` `よくやってる` `好きかも` `人に任せられた`

※画面上でこの2グループを見出しで分ける必要はない。並べるだけでよい。
※前半のほうが先に押せるようになるはず。手放すほうが、新しく何かを出すより近い。`好きかも` は最も遠い（16）。押せる日が来たら、それは大きな一日。

#### 入力の形：皿に積む

**チェックリストにしない。** タップすると、皿の上におもりが積み上がる。

- 消化ではなく蓄積の見た目にする。積む動作が残ることが重要（15）
- **何も積まない日は、空の皿のまま。**空欄ではなく「何も載っていない一日」として形になる。それは失敗ではない
- もう一度タップで下ろせる

#### 厳守

- **目標・達成率・頻度・連続日数を一切表示しない**
- **空白の日には何も表示しない**
- **できなかったこと（いつも通り先回りした等）は記録させない**
- 項目は本人が編集できるようにしておく。飽きた時に自分で手を入れられることが、継続の条件（18）

③の下に置く。順番を逆にすると「良いことを挙げてから不調を報告する」形になり、言い訳の構造になる。

### ⑤ 「置く」

保存でも記録でもなく「置く」。作業ではなく手放す動作にする。

---

## 2. 蓄積ビジュアル

**毎日開く理由はここ。** 開いた瞬間に目に入る位置に、十分な面積で置く。小さく添えない。

- **体の調子**と**心の余白**の2本の波（SVG手書き。グラフライブラリは使わない）
- **頭にあること**は色で示す
- **今日思えたこと**があった日は、波の上に小さな印を重ねる。積んだ数だけ印が増える。**集計はせず、印を置くだけ**

**意味は説明しない。**「木曜だけ色が違う」という気づきは、アプリが指摘するのではなく本人が見つける形にする。

平均・先週比・最高記録の類は出さない。線が上下しているだけ、という見せ方にとどめる。

月が変わっても消さず、積み上げていく。過去はいつでも遡れる。

---

## 3. 予行演習の答え合わせ

**19が出た以上、ここが到達点そのもの。** 条件ではなく実績を安心の根拠にするための機能。
独立したアプリにはせず、アプリAの中に置く。

**仕組み**
「頭にあること」で **`先のこと`** を選んだ記録に、数日後フラグが立つ。
アプリAを開いた時、上部に一件だけ出る。

> 5日前、先のことが頭にあった日。実際どうだった？
> `想像より悪かった` / `同じくらい` / `想像より軽かった` / `そもそも起きなかった`

- タップ一回で終わる。新しく書かせない
- 答えなくても消えるだけ。催促しない
- **「大丈夫ですよ」とは返さない。**慰めは効かない

**予測精度の可視化**
答え合わせが溜まると、内訳が見える。
根性でも心構えでもなく、**データで納得する**設計。

---

## 4. 心配ごとの一覧

認知行動療法の「心配タイム」を、時間を決めない形で実装する。
**時間を固定すると9番の回路に入る**（心配タイムをこなす日々になる）ので、時刻もリマインドも設けない。
置いて、気が向いた時に眺めるだけ。

**「もう心配し終わった」感覚は、同じことが何度も並んでいるのを見た時に自然に出る。**
「またこれか」と気づいた時点で、その心配は一度処理済みになる。
**溜まっていること自体が効く**ので、まとめて考える時間は要らない。

### 表示

**「頭にあること」の一行入力に書かれた文字**が、項目別にグループ化されて溜まる。
`先のこと` だけでなく、全項目が対象。繰り返しは先のことだけとは限らない。

- **同じ内容が繰り返されているのが見えること**が最重要。似た文言をまとめる、または回数を小さく添える
- `先のこと` のものには、答え合わせの結果を横に付ける

表示イメージ：

```
先のこと
  父の病気 ── 11回（起きなかった×6 / 同じくらい×2）
  仕事の締切 ── 8回

子供
  朝の準備でイライラ ── 6回
```

### 厳守

- **アプリ側から「同じことを考えていますね」と指摘しない。** 指摘は解釈にあたる（セクション2）。並べるだけ
- **通知・未読バッジ・件数バッジを出さない。** 溜まっていることを催促されると、処理すべきタスクに変わる
- 見に行くのは気が向いた時だけ。記録画面から一タップで行ける、一段深い場所に置く

---

## 5. 土曜モード

平日は軽いまま。**土曜だけ、記録画面の下に一段追加される。**

曜日で決まるので、自分で「今日はやろう」と判断しなくてよい。型があれば動ける（1）。
週に一度だけ骨がある構成にすることで、毎日5分を要求せずに済む。

### ① 今週の事実

溜まった記録から、事実だけを返す。
例：「今週、先のことが4回」「木曜だけ調子が低かった」

**解釈も助言もしない。**答えなくてよい。

### ② 言語化の練習（1件だけ）

今週の記録から1つ引っ張ってきて、**型を一つだけ**出す。

> 火曜、心の余白20%だった日。この時、体はどうなってた？

型の例：
- この時、体はどうなってた？
- 一番近い言葉を3つの中から選んで、なぜそれか一行で
- これを誰かに一言で伝えるなら
- この時、本当はどうしてほしかった？

**時間差が肝。** その場で書くと咀嚼が走るが、数日経った記録は「他人の記録」に近い距離感になる。距離があると言葉が出る（カウンセリングでは書けていた、という事実に基づく）。

答えなくてよい。書いても一行でよい。穴埋めに近い型から始め、段階的に自由度を上げる。

### ③ 予行演習の答え合わせ

今週 `先のこと` を置いたものが返る。タップ一回（セクション3）。

### 厳守

- **やらなかった週を可視化しない。** 「先週の分」を溜めると未完了になる（8）。その週の分はその日だけ。過ぎたら消える
- **土曜が重い日ならやらなくてよい。** 曜日は本人が変更できるようにしておく
